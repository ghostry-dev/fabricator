# T.null

An [absence](/reference/primitives#absence) primitive: always exactly `null`, with nothing to roll.

Useful as one branch of a [`T.choice`](/reference/primitives/choice), or as the `terminal` value of a field you want to disable entirely. For a value that might be `null` _or_ something else, use [`T.nullable`](/reference/primitives/nullable).
